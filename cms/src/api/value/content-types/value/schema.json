{
  "kind": "collectionType",
  "collectionName": "values",
  "info": {
    "singularName": "value",
    "pluralName": "values",
    "displayName": "Value",
    "description": "A working principle shown in the \"How we work\" section of the About page. Edited in the Strapi admin by the team."
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "description": "Short principle name, e.g. \"No hand-offs\"."
    },
    "text": {
      "type": "text",
      "required": true,
      "description": "One or two sentences explaining the principle."
    },
    "icon": {
      "type": "string",
      "required": true,
      "default": "layers",
      "description": "Icon name — must match a key in the frontend Icon component (frontend/src/components/Icon/icons.tsx). Any string is accepted; unknown names fall back to 'layers' at render time."
    },
    "order": {
      "type": "integer",
      "required": false,
      "description": "Manual sort order in the \"How we work\" section (ascending)."
    }
  }
}
